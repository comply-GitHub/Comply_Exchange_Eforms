import React from 'react'

const Classification = () => {
  return (
    <div>
        This page is goint to be the classification page where user can select FATCA and will move forward with the selections
        This selection will go page by page and can be choosen FATCA categories and at last will select only the sub category of FATCA , which further moves to next step
       This page has 3 to 4 steps to select the respective FATCA category and sub category.
       After FATCA user can select CRS which is another process of selecting category and sub category
    </div>
  )
}

export default Classification